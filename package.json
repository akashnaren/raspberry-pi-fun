{
  "name": "raspberry-pi-fun",
  "version": "0.1.0",
  "private": true,
  "description": "AI Studio Stage 1 — the fishbowl. A self-running office of AI employees on a Raspberry Pi.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --test-reporter=spec test/*.test.js"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
